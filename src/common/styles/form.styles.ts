import { StyleSheet } from "react-native";

const FormStyle = StyleSheet.create({
    formContainer: {
        width: '100%'
    },
    inputContainer: {
        width: '100%',
        marginBottom: 20,
        gap: 5,
    },
    valiTextFalse: {
        color: '#FF0000'
    },
})

export default FormStyle;